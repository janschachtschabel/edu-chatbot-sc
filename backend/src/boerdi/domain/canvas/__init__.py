"""Pure canvas-domain logic (framework-free).

Port target for the ALT ``canvas_*`` pure helpers (postprocess, and later the
intent/type getters) that ``services/canvas_service`` composes into
``generate_canvas_content``. Mirrors the ``domain/cards`` sibling grouping.
"""
